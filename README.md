# Telugunudi

Spring Boot project for a Telugu language, culture, and history website.

## Run locally

Prerequisites:
- Java 17+
- Maven (or use `mvnw` later)

Run:

```bash
mvn spring-boot:run
```

Then open:
- `http://localhost:8080/`

